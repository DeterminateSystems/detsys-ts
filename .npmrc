# eslint-plugin-github depends on eslint-plugin-import, which still caps its
# eslint peer range at ^9 even though eslint 10 lints this repo clean. Relax
# peer resolution so `npm ci` and `npm install` work without a flag.
#
# Remove this once eslint-plugin-import declares eslint ^10 support.
legacy-peer-deps=true
